#!/usr/bin/env python3
import sys

# Get the number of parameters (excluding the script name)
num_params = len(sys.argv) - 1

# Print the number of parameters followed by a newline
print(num_params, "\n")